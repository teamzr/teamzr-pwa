import * as React from 'react';
import propTypes from 'prop-types';

import DefaultLayout from '../pagesLayouts/DefaultLayout';

function Messages(props) {
  return <DefaultLayout>Messages</DefaultLayout>;
}

export default Messages;
