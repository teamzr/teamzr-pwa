import { makeStyles } from '@material-ui/core';

export const useLoginPageStyle = makeStyles(
  (theme) => (
    {
      logo: {
        width: '125px',
        height: '125px',
      },
    },
    { index: 1 }
  )
);
